import { ConverterFields } from "../ConverterFields/ConverterFields";
import { FastSelectors } from "../FastSelectors/FastSelectors";

export const Converter = () => {
  return (
    <div>
      <ConverterFields />
      <FastSelectors />
    </div>
  );
};
